# R Packages {#sec-r-packages}

```{r}
library(ggplot2)
#| echo: false
source("_formatting.R")
```

R is a powerful tool because 

```{r, eval=FALSE, echo=TRUE}

install.packages(c("tidyverse", "curl", "ragnar", "duckdb", "shiny", "shinyjs"))

```

