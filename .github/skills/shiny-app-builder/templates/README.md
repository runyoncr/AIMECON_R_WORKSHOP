# {{APP_TITLE}}

This local Shiny app extends the workshop example in `{{SOURCE_PAGE}}`.

## Purpose

{{TEACHING_CLAIM}}

## Requirements

- R
- The `shiny` package
- {{OTHER_REQUIREMENTS}}

Install missing packages in your usual R environment before running the app. The app does not install packages itself.

## Run Locally

1. Extract the complete ZIP archive.
2. Open R in the extracted `{{APP_SLUG}}` folder.
3. Run:

```r
shiny::runApp()
```

The app runs on your computer and does not require credentials or an external service.

## Controls and Outputs

{{CONTROLS_AND_OUTPUTS}}

## Data and Limitations

{{DATA_PROVENANCE}}

{{STATISTICAL_LIMITATIONS}}
